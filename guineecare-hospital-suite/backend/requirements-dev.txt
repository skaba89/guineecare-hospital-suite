# Dev-only dependencies — not needed at runtime.
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Load testing
locust==2.32.8

# SAST & dependency scanning (already in CI, useful locally)
bandit==1.9.4
pip-audit
